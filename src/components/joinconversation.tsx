import React from 'react';

const JoinConversationButton = () => {
  return (
    <button className='animate-flash rounded-lg px-4 py-2 font-semibold shadow-lg hover:bg-blue-600 dark:text-white'>
      Join the conversation
    </button>
  );
};

export default JoinConversationButton;
